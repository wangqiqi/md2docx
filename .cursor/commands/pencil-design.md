---
description: 【工具】Pencil CLI 视觉设计 — 自然语言生成 .pen 与导出图
---

加载 skill **pencil-design**：

1. 检查 `pencil` CLI 与登录状态（`pencil status`）
2. 用用户原话作 `--prompt`，生成 `.pen` 并 `--export` PNG
3. Read 导出图展示给用户；迭代用 `--in` 载入上一版

**不是** **ia**（信息架构）· **不是** **delivery**（上线验收）· 编辑器内改 `.pen` 用 Pencil MCP（若已装）。
