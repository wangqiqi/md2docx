# md2docx 项目代码审查报告

**审查日期**: 2026-06-08  
**项目版本（审查时）**: 0.4.3  
**闭合复核**: 2026-07-09 · 代码 **v0.5.44** · **275 passed**  
**审查范围**: 核心转换器（`src/mddocx/converter/`）、CLI（`cli.py`）、WebUI（`webui/`）、测试（`tests/` + `webui/tests/`）、CI/CD（`.github/workflows/`）、配置（`pyproject.toml`）、文档（`docs/`、`README.md`、`CHANGELOG.md`）、脚本（`scripts/`）

> **状态（DOC-REV-01）**：下文 Critical / Major / Minor / Suggestions **主体均已闭合**。闭合证据见 `archive/sprint/`；**仍开放**仅 ROI 候选 → [`.cursorGrowth/plan.md`](.cursorGrowth/plan.md)（**M-MYPY-02** · **ARCH-R03**）。`M-MERMAID-03`（state/class/pie）已于 v0.5.36 闭合，不再列入开放项。

---

## 执行摘要

md2docx（包名 `mddocx`）是一个结构清晰、功能较完整的 Markdown 转 DOCX 工具，采用「基础转换器 + 元素转换器」的可扩展架构，测试覆盖面较广，CI 流水线基本完备。本次审查共发现 **4 个严重问题**、**12 个重要问题**、**10 个一般问题** 及若干改进建议。

**总体评价（审查时）**: 核心转换逻辑质量良好，单元/集成测试在本地可通过（139 passed, 2 skipped），但 WebUI 存在状态复用缺陷、依赖声明遗漏、安全加固不足，且文档与实现存在多处不一致。

**总体评价（2026-07-09 复核）**: P0–P2 行动项与 C/M/m/S 主体已交付；当前基线 **v0.5.44** · **275 passed**。开放债见 plan ROI 候选。

**建议优先级（历史 · 已执行）**:
1. P0：修复 `BaseConverter` 状态复用 / WebUI 单例问题；补全 `requests` 依赖 → ✅
2. P1：修复 CLI 重复 `parse_args`；加固 WebUI 安全（XSS、CSRF、SSRF） → ✅
3. P2：统一文档与版本信息；将 WebUI 测试纳入 CI；提升覆盖率 → ✅

---

## 严重问题 (Critical) — ✅ 已闭合

> 闭合：`archive/sprint/` P0 相关 · M-CONV 等；见 §推荐行动计划第一阶段。

### C1. WebUI 使用全局单例转换器，导致多次转换内容累积 — ✅ 已闭合

**文件**: `src/mddocx/webui/app.py` 第 48、126 行  
**文件**: `src/mddocx/converter/base.py` 第 63、100-122 行

WebUI 在模块级创建单个 `BaseConverter()` 实例：

```python
converter = BaseConverter()  # app.py:48
doc = converter.convert(markdown_content)  # app.py:126
```

而 `BaseConverter.convert()` 每次调用都向**同一个** `self.document` 追加内容，不会重置文档或内部状态（`_list_stack`、代码块 `_last_was_code`、图片缓存等）。用户在 WebUI 中第二次转换时，输出 DOCX 将包含第一次及之后所有历史内容。

CLI 和 `batch_convert.py` 每次新建 `BaseConverter()`，因此不受影响。

**建议**:
- 每次请求创建新实例：`doc = BaseConverter().convert(markdown_content)`
- 或在 `convert()` 开头重置：`self.document = Document()` 并清空 `_list_stack` 等状态
- 增加 WebUI 多请求连续转换的集成测试

---

### C2. `requests` 库未声明为项目依赖，在线图片功能在干净安装下会失败 — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/image.py` 第 10、183 行  
**文件**: `pyproject.toml` 第 34-39 行

`ImageConverter._get_image_data()` 使用 `requests.get()` 下载远程图片，但 `pyproject.toml` 的 `dependencies` 中未包含 `requests`。通过 `pip install mddocx` 安装后，导入或运行时会触发 `ModuleNotFoundError`（测试通过是因为 mock 了 `requests.get`）。

**建议**: 在 `pyproject.toml` 添加 `requests>=2.28.0`（并考虑设置超时、大小限制）。

---

### C3. `BaseConverter` 设计上非无状态，复用实例会产生隐性数据污染 — ✅ 已闭合

**文件**: `src/mddocx/converter/base.py`  
**文件**: `tests/conftest.py` 第 35-47 行  
**文件**: `tests/integration/test_full_conversion.py` 第 10-25 行

除文档累积外，以下状态在多次 `convert()` 间不会重置：
- `_list_stack`（列表嵌套栈）
- `CodeConverter._last_was_code`
- `ImageConverter._image_cache`（无限增长）

`test_convert_all_samples` 在同一 `converter` fixture 上循环转换多个样例文件，只断言「文件存在且非空」，未验证内容独立性，因此未能发现此缺陷。`conftest.py` 的 `converter` fixture 虽重置了 `document`，但未重置其他状态，且注册的转换器键名不完整（见 M8）。

**建议**: 在 `convert()` 入口统一重置所有可变状态，或文档明确要求「每次转换必须新建实例」。

---

### C4. 本地图片路径未做安全校验，存在任意文件读取风险 — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/image.py` 第 190-197 行

```python
if os.path.exists(src):
    with open(src, "rb") as f:
        image_data = f.read()
```

Markdown 中 `![](../../etc/passwd)` 等形式可尝试读取服务器上任意可读文件（在 WebUI 或 CLI 运行于有权限的目录时）。结合在线图片的 SSRF（见 M5），安全风险较高。

**建议**:
- 限制图片路径必须在 Markdown 文件所在目录或其子目录内
- 使用 `pathlib.Path.resolve()` 校验解析后的路径在允许根目录下
- 禁止 `http://127.0.0.1`、`http://169.254.x.x` 等内网地址（SSRF 防护）

---

## 重要问题 (Major) — ✅ 已闭合

> 闭合：P1/P2 行动计划 · `archive/sprint/`（M-DOC-* · T-TEST-* · M-HTML-* 等）。

### M1. CLI 存在重复 `parse_args()` 调用 — ✅ 已闭合

**文件**: `src/mddocx/cli.py` 第 170、212、214 行

```python
args, remaining = parser.parse_known_args()  # 170
args = parser.parse_args(remaining)          # 212
args = parser.parse_args()                   # 214 ← 覆盖上一行结果
```

第 214 行完全覆盖第 212 行的解析结果，属于冗余/死代码。当前因两次都解析完整 `sys.argv` 而功能尚可，但增加维护风险，且与「两阶段解析以支持 `--lang`」的设计意图不一致。

**建议**: 删除第 214 行，仅保留 `args = parser.parse_args(remaining)`，并补充 `--lang en` 的 CLI 测试。

---

### M2. WebUI 预览接口存在存储型 XSS 风险 — ✅ 已闭合

**文件**: `src/mddocx/webui/app.py` 第 207-210、218-229 行  
**文件**: `src/mddocx/webui/static/js/app.js` 第 537 行

服务端 `/preview` 将 `markdown-it` 渲染的 HTML 直接拼入响应；客户端通过 `previewContent.innerHTML = html` 插入 DOM。若 Markdown/HTML 输入含恶意脚本，可在浏览器中执行。

`docs/04_API与部署.md` 第 104 行声称「XSS 防护（通过 Flask 模板转义）」，但预览接口返回的是 HTML 片段而非 Jinja 模板渲染，该描述不准确。

**建议**:
- 使用 `bleach` 等库对预览 HTML 消毒
- 或改用 `textContent` / iframe sandbox 隔离预览
- 添加 CSP 头（`Content-Security-Policy`）

---

### M3. WebUI 缺少 CSRF 防护 — ✅ 已闭合

**文件**: `src/mddocx/webui/app.py` 第 85、178 行  
**文件**: `src/mddocx/webui/README.md` 第 74 行（已注明「推荐添加」但未实现）

`/convert` 和 `/preview` 均为 POST 且无 CSRF token。在默认绑定 `0.0.0.0` 时，局域网内可能被跨站请求滥用。

**建议**: 集成 `Flask-WTF` 或实现 CSRF token 校验。

---

### M4. 默认监听 `0.0.0.0` 且开发配置使用固定 SECRET_KEY — ✅ 已闭合

**文件**: `src/mddocx/webui/config.py` 第 12、27-29 行  
**文件**: `src/mddocx/webui/start_webui.py` 第 25 行

- `HOST` 默认为 `0.0.0.0`，暴露在所有网络接口
- `SECRET_KEY` 默认为 `"dev-secret-key-change-in-production"`
- `start_webui.py` 硬编码 `debug=True`

**建议**: 开发环境默认 `127.0.0.1`；生产环境强制环境变量；`start_webui.py` 读取 `config.DEBUG`。

---

### M5. 在线图片下载存在 SSRF 风险 — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/image.py` 第 182-188 行

`requests.get(src, timeout=10)` 未校验目标 URL，可请求内网/metadata 服务（如 `http://169.254.169.254/`）。

**建议**: URL 白名单、禁止私有 IP 段、限制响应体大小。

---

### M6. CI 未运行 WebUI 测试，与 README 描述不符 — ✅ 已闭合

**文件**: `.github/workflows/ci.yml` 第 38-40 行  
**文件**: `pyproject.toml` 第 6-8 行（`testpaths = ["tests"]`）

CI 仅执行 `pytest --cov=src`，不包含 `src/mddocx/webui/tests/`（12 个用例）。本地全量测试为 139 passed，CI 约为 127 passed。

README 第 51 行写「Web 界面测试 - 完整的用户界面功能验证」，但实际未纳入 CI。

**建议**:
```yaml
python -m pytest tests/ src/mddocx/webui/tests/ --cov=src --cov-report=xml
```

---

### M7. 文档声称的测试数量与覆盖率与实际不符 — ✅ 已闭合（基线持续随版本更新）

| 来源                           | 声称                   | 实际（本次审查）                  |
| ------------------------------ | ---------------------- | --------------------------------- |
| README 第 47 行                | 19 个测试用例          | 141 个（139 passed + 2 skipped）  |
| README 第 69 行                | 89 个测试，85%+ 覆盖率 | 全量 139 passed；覆盖率约 **67%** |
| docs/04_API与部署.md 第 104 行 | XSS 已防护             | 预览接口未消毒                    |
| CHANGELOG                      | 最新 0.4.2             | 代码版本 0.4.3，缺少 0.4.3 条目   |

**建议**: 统一更新 README、CHANGELOG、docs，并以 CI 输出的覆盖率徽章为准。

---

### M8. `requirements-prod.txt` / `requirements.txt` 不存在 — ✅ 已闭合

**文件**: `README.md` 第 93、137、151-153、273-275 行

README 多处引用 `requirements-prod.txt`、`requirements.txt`（含符号链接说明），但仓库中**不存在**这些文件（仅依赖 `pyproject.toml`）。

**建议**: 删除过时引用，或生成并维护 `requirements-prod.txt`。

---

### M9. `tests/conftest.py` 中 `converter` fixture 配置不完整 — ✅ 已闭合

**文件**: `tests/conftest.py` 第 35-47 行

该 fixture：
- 用 `"paragraph"` 注册 `TextConverter`，而 `BaseConverter` 使用键名 `"text"`
- 未注册 `link`、`image`、`table`、`hr`、`task_list`、`html` 等转换器
- 手动 `HeadingConverter()` 未传入 `base_converter`

依赖此 fixture 的集成测试（如 `test_full_conversion.py`）并未覆盖完整转换能力。

**建议**: 统一使用 `base_converter` fixture，或让 `converter` 直接 `return BaseConverter()`。

---

### M10. CHANGELOG 缺少 0.4.3 版本记录 — ✅ 已闭合（后续版本按工作流维护）

**文件**: `CHANGELOG.md` 第 8 行  
**文件**: `pyproject.toml` 第 13 行

代码与 README 均为 0.4.3，CHANGELOG 最新条目仍为 0.4.2（2025-12-26），版本管理流程（`docs/07_版本工作流.md`）未完全执行。

---

### M11. README 中多处命令与路径错误 — ✅ 已闭合

**文件**: `README.md`

| 行号 | 问题                                                                |
| ---- | ------------------------------------------------------------------- |
| 169  | `python -m webui.app` 应为 `python -m mddocx.webui.app`             |
| 172  | `from webui.app import app` 应为 `from mddocx.webui.app import app` |
| 205  | `python -m src.cli` 应为 `mddocx` 或 `python -m mddocx.cli`         |
| 211  | `batch_convert_test.py` 不存在，实际为 `scripts/batch_convert.py`   |
| 270  | 项目结构中脚本名错误                                                |

---

### M12. `html2docx` 为可选依赖但未文档化，HTML 转换覆盖率偏低 — ✅ 已闭合（html-for-docx · M-HTML-01）

**文件**: `src/mddocx/converter/elements/html.py` 第 14-19、75-192 行  
**覆盖率**: `html.py` 仅 **60%**（242 行中 97 行未覆盖）

`html2docx` 未列入 `pyproject.toml` 的 optional-dependencies，相关测试被 skip（`test_convert_with_html2docx SKIPPED`）。复杂 HTML 回退到简陋的正则解析或剥离标签。

---

## 一般问题 (Minor) — ✅ 已闭合

> 闭合：[`archive/sprint/20260608_225306_审查残余代码_M-CONV-01_Sprint闭合_打版_v0.4.10.md`](archive/sprint/20260608_225306_审查残余代码_M-CONV-01_Sprint闭合_打版_v0.4.10.md) · [`archive/sprint/20260609_082500_Minor_M-MINOR-01_Sprint闭合_打版_v0.5.30.md`](archive/sprint/20260609_082500_Minor_M-MINOR-01_Sprint闭合_打版_v0.5.30.md)

### m1. `BaseConverter.convert()` 异常处理过于宽泛 — ✅ 已闭合

**文件**: `src/mddocx/converter/base.py` 第 440-442 行

所有未预期异常统一包装为 `ConvertError`，丢失原始堆栈类型，不利于调试。

---

### m2. `BlockquoteConverter` 功能不完整 — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/blockquote.py` 第 51-83 行

引用块内不支持链接、删除线、行内代码等（仅 text/strong/em/softbreak），与 `TextConverter` 能力差距大。

---

### m3. `TableConverter` 对齐使用魔法数字 — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/table.py` 第 399-404 行

```python
paragraph.alignment = 0  # 应使用 WD_PARAGRAPH_ALIGNMENT.LEFT
```

降低可读性，且与项目其他模块风格不一致。

---

### m4. `TableConverter.__init__` 未向父类传递 `base_converter` — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/table.py` 第 22-23 行

`super().__init__()` 未传参，之后手动赋值 `self.base_converter`，与其他转换器不一致。

---

### m5. WebUI `app.py` 中冗余的 `sys.path` 操作 — ✅ 已闭合

**文件**: `src/mddocx/webui/app.py` 第 17-19 行

包已通过 setuptools 安装，`sys.path.insert` 在正式入口中不应需要，增加导入歧义。

---

### m6. 预览与转换使用不同的 MarkdownIt 配置 — ✅ 已闭合

**文件**: `src/mddocx/webui/app.py` 第 29 行 vs `src/mddocx/converter/base.py` 第 57-62 行

WebUI 预览用默认 `MarkdownIt()`，转换用 `commonmark` + `strikethrough` + `table` + `html`。用户看到的预览与最终 DOCX 可能不一致。

---

### m7. `test_full_conversion.py` 存在重复模块文档字符串 — ✅ 已闭合

**文件**: `tests/integration/test_full_conversion.py` 第 1-7 行

两个三引号 docstring 连续出现，第二个覆盖第一个，属代码整洁问题。

---

### m8. `docs/01_架构设计.md` 项目结构过时 — ✅ 已闭合

**文件**: `docs/01_架构设计.md` 第 13-48 行

未包含 `webui/` 模块、`mddocx` 包名、CI 工作流等当前结构；仍写 `src/converter/` 而非 `src/mddocx/converter/`。

---

### m9. `docs/03_测试指南.md` 导入路径过时 — ✅ 已闭合

**文件**: `docs/03_测试指南.md` 第 54 行

示例使用 `from src.converter.elements.heading import HeadingConverter`，应为 `from mddocx.converter.elements.heading import ...`。

---

### m10. 图片缓存无上限 — ✅ 已闭合

**文件**: `src/mddocx/converter/elements/image.py` 第 24、177-178 行

`_image_cache` 字典持续增长，长时间运行的 WebUI 实例可能内存膨胀。

---

## 建议改进 (Suggestions) — ✅ 主体已闭合 · 残余见开放项

> 闭合主体：[`archive/sprint/20260609_080200_架构建议_M-ARCH-01_Sprint闭合_打版_v0.5.26.md`](archive/sprint/20260609_080200_架构建议_M-ARCH-01_Sprint闭合_打版_v0.5.26.md) · mypy 渐进 [`…M-MYPY-01…`](archive/sprint/20260609_081200_mypy_M-MYPY-01_Sprint闭合_打版_v0.5.29.md) · 分块 [`…M-PERF-01…`](archive/sprint/20260609_100000_大文件分块_M-PERF-01_Sprint闭合_打版_v0.5.33.md)

1. **架构**: TokenProcessor 拆分 — ✅ 已闭合（M-ARCH-01 / v0.5.26）
2. **类型安全**: 严格 mypy — ✅ 渐进闭合（M-MYPY-01）；**残余** elements `--strict` → 候选 **M-MYPY-02**
3. **性能**: 大文件分块 + WebUI 限流 — ✅ 已闭合（M-PERF-01 · M-ARCH-01）
4. **可观测性**: 结构化日志已有；**残余** metrics 面 → 候选 **ARCH-R03**
5. **Docker**: Dockerfile — ✅ 已闭合（M-ARCH-01）
6. **预提交**: CONTRIBUTING / pre-commit 文档 — ✅ 已闭合（M-ARCH-01）
7. **任务列表**: Word checkbox — ✅ 已闭合（M-ARCH-01）
8. **统一错误类型**: CLI/WebUI 错误码 — ✅ 已闭合（M-ARCH-01）

---

## 优点 (Strengths)

1. **清晰的模块化架构**: `BaseConverter` + `ElementConverter` 子类，职责分离良好，扩展新元素类型成本低。
2. **较完整的 Markdown 支持**: 标题、列表（含嵌套）、表格、代码块、链接、图片、引用、HR、任务列表、基础 HTML 均有对应转换器。
3. **测试体系扎实**: 141 个测试用例覆盖单元、集成、边界、大文件、错误处理；元素级测试粒度合理。
4. **CI/CD 基础设施完善**: 多 Python 版本矩阵（3.8-3.12）、flake8/black/isort 质量门禁、构建与 PyPI 发布分离。
5. **WebUI 用户体验**: 响应式布局、防抖预览、文件上传验证、安全 HTTP 头、临时文件自动清理等考虑周到。
6. **CLI 国际化**: `--lang zh/en` 双语言帮助设计有远见（尽管实现有小瑕疵）。
7. **错误处理层次**: 自定义 `MD2DocxError` / `ParseError` / `ConvertError` 异常体系，CLI 有文件占用重试逻辑。
8. **链接处理完善**: `LinkConverter` 支持段落内超链接、样式叠加及 OOXML 关系映射，实现较为完整。

---

## 测试与质量

### 测试执行结果

| 范围                                         | 结果                                                               |
| -------------------------------------------- | ------------------------------------------------------------------ |
| `tests/` + `src/mddocx/webui/tests/`（全量） | **139 passed, 2 skipped**（约 1.53s）                              |
| `tests/` only（与 CI 一致）                  | **127 passed, 2 skipped**                                          |
| Skipped                                      | `test_convert_with_html2docx`（缺少 html2docx）、另 1 个 html 相关 |

### 代码覆盖率（`pytest --cov=src`，仅 tests/）

| 模块                  | 覆盖率                           |
| --------------------- | -------------------------------- |
| 整体                  | **67%**（1992 语句，657 未覆盖） |
| `converter/base.py`   | 89%                              |
| `elements/heading.py` | 100%                             |
| `elements/html.py`    | 60%                              |
| `elements/image.py`   | 76%                              |
| `webui/app.py`        | 0%（未被 CI 执行）               |
| `webui/config.py`     | 0%                               |
| `cli.py`              | 77%                              |

### 测试缺口

- WebUI 多请求连续转换（状态累积）
- CLI `--lang` 参数端到端
- 安全：XSS、SSRF、路径遍历
- `converter` fixture 与生产路径不一致
- 在线图片在真实网络环境下的集成测试

---

## 文档一致性

| 文档                    | 状态       | 主要偏差                                                          |
| ----------------------- | ---------- | ----------------------------------------------------------------- |
| `README.md`             | ⚠️ 过时     | 测试数量、脚本名、模块路径、requirements 文件、重复「已完成」段落 |
| `CHANGELOG.md`          | ⚠️ 滞后     | 缺少 0.4.3；最新为 0.4.2                                          |
| `docs/01_架构设计.md`   | ⚠️ 过时     | 无 webui、包名错误                                                |
| `docs/04_API与部署.md`  | ⚠️ 部分不准 | XSS 声明、部署路径、Docker 未实现                                 |
| `docs/03_测试指南.md`   | ⚠️ 过时     | 导入路径、覆盖率目标                                              |
| `pyproject.toml`        | ✅ 准确     | 版本 0.4.3                                                        |
| `docs/07_版本工作流.md` | ✅ 较新     | 与 CHANGELOG 执行不同步                                           |

---

## 总结与行动计划

### 问题统计

| 严重程度            | 数量 |
| ------------------- | ---- |
| Critical（严重）    | 4    |
| Major（重要）       | 12   |
| Minor（一般）       | 10   |
| Suggestions（建议） | 8    |

### 推荐行动计划

> **同步说明（2026-07-09 · DOC-REV-01）**：P0/P1/P2 与 C/M/m/S **主体已闭合**（证据 `archive/sprint/`）。  
> **仍开放（ROI）** — 真源 [`.cursorGrowth/plan.md`](.cursorGrowth/plan.md)：
> 1. **M-MYPY-02** — `converter/elements` 类型收紧 / `--strict`
> 2. **ARCH-R03** — 可观测性 metrics（不止现有 `duration_ms` 日志）  
> ~~M-MERMAID-03~~ 已于 v0.5.36 闭合（state/class/pie），**不再开放**。

**第一阶段（P0，1-2 天）** — ✅ 已闭合
- [x] 修复 `BaseConverter` 状态复用：每次转换重置文档与内部状态
- [x] WebUI 改为 per-request 实例化转换器
- [x] `pyproject.toml` 添加 `requests` 依赖
- [x] 图片路径校验 + SSRF 基础防护

**第二阶段（P1，3-5 天）** — ✅ 已闭合
- [x] 删除 CLI 重复 `parse_args()`（`cli.py:214`）
- [x] 预览 HTML 消毒 + CSP 头
- [x] 添加 CSRF 防护
- [x] CI 纳入 `src/mddocx/webui/tests/`
- [x] 更新 CHANGELOG、修正 README（持续同步见 M-DOC-04）

**第三阶段（P2，持续改进）** — ✅ 已闭合
- [x] 修复 `conftest.py` converter fixture
- [x] 统一预览与转换的 MarkdownIt 配置（dollarmath 已对齐，v0.5.30+）
- [x] 提升 html.py 覆盖率；文档化 html-for-docx 可选依赖
- [x] 重构 `base.py` token 处理逻辑（TokenProcessor · v0.5.26）
- [x] 补齐 docs/01_架构设计.md / 03_测试指南.md（T-TEST-02 已更新基线与覆盖率目标）

---

*本报告基于 2026-06-08 的源码静态审查及本地 pytest。**DOC-REV-01（2026-07-09）** 已标注闭合状态并与 plan ROI 候选对齐。*
